---
task: 403-debug-data
date: 2026-05-19
agent: data-analyst
type: plan
based_on: analysis/2026-05-19_403-diagnosis.md
---

# Implementation Plan: Fix 403 Forbidden Error

## Current State
- Root `.htaccess` exists and disables directory listing.
- Root `.htaccess` routes all non-existent files/dirs to `index.php` in the root.
- `index.php` is missing from the root; it resides in `public/index.php`.
- `public/.htaccess` is missing.
- Result: 403 Forbidden on root access.

## Target State
- Application is accessible via root URL without `/public/` in the path.
- Static assets in `public/` are served correctly from the root URL.
- Secure files (like `.env`) remain inaccessible.
- `public/.htaccess` is restored to standard Laravel configuration.

## Steps
1. **Update Root `.htaccess`**:
   - Modify the rewrite rules to first check if the requested file exists in the `public/` directory.
   - Update the fallback rewrite rule to point to `public/index.php` instead of `index.php`.
2. **Restore `public/.htaccess`**:
   - Create the missing `public/.htaccess` file with the standard Laravel rewrite rules to ensure internal routing within the `public` directory works correctly.
3. **Verification**:
   - Test root URL access.
   - Test access to a static asset (e.g., a CSS or JS file in `public/js/`).
   - Verify that sensitive files (e.g., `.env`) still return 403/404.

## Dependencies
- Apache `mod_rewrite` must be enabled (already assumed as `.htaccess` is being used).

## Blockers/Challenges
| Blocker | Solution |
|---------|----------|
| Asset Pathing | Ensure the root `.htaccess` correctly maps requests for assets to the `public/` folder before falling back to `index.php`. |

## Risks & Mitigations
| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Rewrite Loop | Low | High | Use `RewriteCond %{REQUEST_FILENAME} !-f` and `!-d` to prevent looping on existing files. |
| Asset 404s | Medium | Medium | Implement the "Check `public/` first" rule in root `.htaccess`. |

## Next Steps
1. Apply changes to `.htaccess`.
2. Create `public/.htaccess`.
3. Validate the fix.

---
*Generated: 2026-05-19 15:30*
