# Daily Log - 2026-05-16

## Task: Global Rules & Memory Awareness Enforcement
- **Objective**: Ensure all agents and subagents strictly follow global rules defined in `AGENTS.md` and utilize `MEMORY.md` for context.
- **Actions Taken**:
    - Modified `kilo.jsonc` to add `"instructions": ["AGENTS.md", "SOUL.md", "MEMORY.md"]`. This ensures system-level loading of global instructions for every agent session.
    - Batch updated 55 agent prompt files (45 in `agent/` and 10 in `agent/trading/`) to include an explicit "Global Rules" reference blockquote.
    - Updated `AGENTS.md` to mandate a **Memory Screening** process: Agents must now read `MEMORY.md` and identify relevant references in `memory/refs/` before proceeding with any task.
- **Outcome**: Subagents are no longer "blind" to global rules and are now procedurally required to screen long-term memory for project context.
